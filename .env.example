# Use `/api` when frontend and backend are served from the same origin (recommended for production).
VITE_API_BASE=http://localhost:3001/api

# OSRM routing endpoint. In dev we proxy `/osrm` via `vite.config.js`.
# For production, either set up a reverse proxy or point to a public/self-hosted OSRM instance.
VITE_OSRM_BASE=/osrm

# Optional demo behavior: loop the post feed when it reaches the end.
VITE_LOOP_FEED=false
